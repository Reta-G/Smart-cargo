{
  "name": "cargo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Cargo logistics app — monorepo root",
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "install:all": "npm install --prefix server && npm run db:generate --prefix server && npm install --prefix client",
    "lint": "npm run lint --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
